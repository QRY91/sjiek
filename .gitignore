# Go build output
sjiek
sjiek_binary 
*.exe 

# Test repository (if it's inside the project and you don't want to track its content)
# However, your setup_test_repo.sh *creates* it, so maybe you don't want to ignore the script itself.
# If sjiek_test_repo is generated by the script and you don't want to commit its *contents*:
tests/sjiek_test_repo/ 

# OS-specific files
.DS_Store
Thumbs.db
